name: Deploy to EC2 
on:  
  push:  
    branches:  
 -       shaindy 
jobs:  
  deploy:  
    runs-on: ubuntu-latest 
    steps:  
 -       name: Deploy to Server 
        uses: appleboy/ssh-action@v0.1.10 
         with:  
          host: ${{ secrets.SERVER_IP_shaindy }} 
          username: ${{ secrets.SERVER_USER }} 
           key: ${{ secrets.SSH_PRIVATE_KEY_shaindy }} 
          script|  : 
             # עדכון הקוד מהגיט בשרת
             cd /home/ubuntu/KonditoriaApp 
            git checkout shaindy 
            git pull origin shaindy 
 )GitHub            # בנייה והעתקה של הקליינט (בהנחה שהרצת הבילד ב-
          # אם את רוצה לבנות ישירות על השרת כדי לחסוך העברת קבצים :
             cd ./react 
            npm install 
            npm run build 
             sudo cp -r dist/* /var/www/html/  
             # אתחול הסרבר
             sudo systemctl restart konditoria 
